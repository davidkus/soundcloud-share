<h2 class="ui dividing header"><%= t('rooms.edit_room') %></h2>

<%= render 'form' %>

<%= link_to t('common.show'), room , class: "ui button"%>
<%= link_to t('common.back'), :back, class: "ui button" %>
